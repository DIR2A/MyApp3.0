<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApp">
        <activity
            android:name=".catalog.CatalogAccessories"
            android:exported="false"
            android:label="@string/accessory_multiple"
            android:parentActivityName=".first.Catalog"/>
        <activity
            android:name=".catalog.CatalogHelmetsMasks"
            android:exported="false"
            android:label="@string/helmets_masks"
            android:parentActivityName=".first.Catalog" />
        <activity
            android:name=".catalog.CatalogClothes"
            android:exported="false"
            android:label="@string/clothes"
            android:parentActivityName=".first.Catalog" />
        <activity
            android:name=".catalog.CatalogSki"
            android:exported="false"
            android:label="@string/mount_ski_multiple"
            android:parentActivityName=".first.Catalog" />
        <activity
            android:name=".catalog.CatalogSnowboards"
            android:exported="false"
            android:label="@string/snowboard_single"
            android:parentActivityName=".first.Catalog" />
        <activity
            android:name=".first.AboutUs"
            android:exported="false"
            android:label="@string/about_us" />
        <activity
            android:name=".first.Liked"
            android:exported="false"
            android:label="@string/liked" />
        <activity
            android:name=".first.Catalog"
            android:exported="false"
            android:label="@string/catalog" />
        <activity
            android:name=".first.Cart"
            android:exported="false"
            android:label="@string/cart" />
        <activity
            android:name=".additional.Preview"
            android:exported="false"
            android:theme="@style/Theme.NoActionBar" />
        <activity
            android:name=".first.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>